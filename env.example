# Copie para .env e preencha (nunca suba o .env real)
# No GitHub: Settings → Secrets and variables → Actions → New repository secret

N8N_WEBHOOK_URL=https://n8n.data-skywalker.com/webhook/anvisa-pipeline
